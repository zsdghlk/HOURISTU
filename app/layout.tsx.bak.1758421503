export const metadata = { title: "六法全書", description: "直感的に探せる・読みやすい法令ブラウザ" };

import "./globals.css";
import Link from "next/link";

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ja">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="" />
        <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;600;700&display=swap" rel="stylesheet" />
      </head>
      <body>
        <header className="site-header fixed top-0 left-0 right-0 z-50 bg-white shadow">
          <div className="container row">
            <Link href="/" className="brand">六法全書</Link>
            <nav className="nav">
              <Link href="/" className="no-underline">ホーム</Link>
              <span id="law-tabs-slot" />
            </nav>
          </div>
        </header>
        <div className="h-16" aria-hidden="true"></div>
        <div className="h-14" aria-hidden="true"></div>

        <main className="container pt-16">{children}</main>

        <footer className="site-footer">
          <div className="container row">
            <span>© {new Date().getFullYear()} 六法全書</span>
            <span>・</span>
            <Link href="/#laws" className="no-underline">法令一覧</Link>
          </div>
        </footer>
      </body>
    </html>
  );
}
